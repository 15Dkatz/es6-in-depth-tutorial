function count(x) {
  var numbers = [];
  if () { // TODO check that x is even
    for () { // TODO assign i to 0; give i the limit of x; and increment by 2
      numbers.push(i);
    }
  } else {
    for () { // TODO assign i to 1; give i the limit of x; and increment by 2
      numbers.push(i);
    }
  }
  return numbers;
}
